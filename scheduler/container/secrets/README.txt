Put all of your secrets in this directory as files ending with .secret. 
The file name (without the .secret extension) will be the secret key.

You can use the filesystem permissions to restrict access.

The entire file contents are the secret value. Most secrets will be small, one-line files.